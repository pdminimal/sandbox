{
  "name": "sandbox",
  "version": "0.0.1",
  "description": "sandbox",
  "main": "index.js",
  "scripts": {
    "test": "jest .",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pdminimal/sandbox.git"
  },
  "author": "pdminimal",
  "license": "0BSD",
  "bugs": {
    "url": "https://github.com/pdminimal/sandbox/issues"
  },
  "homepage": "https://github.com/pdminimal/sandbox#readme",
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "babel-jest": "^24.7.1",
    "gts": "^0.9.0",
    "jest": "^24.7.1",
    "sass": "^1.17.4",
    "ts-jest": "^24.0.1",
    "typescript": "~3.1.0"
  },
  "jest": {
    "transform": {
      "^.+\\.jsx?$": "babel-jest",
      "^.+\\.tsx?$": "ts-jest"
    },
    "testPathIgnorePatterns": ["/node_modules/", "build", "dist"]
  }
}
